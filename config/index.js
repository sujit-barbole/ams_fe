console.log('Attempting to load API_URL and DEALER_ID');
import { API_URL, DEALER_ID, DEALER_UID } from '@env';
console.log('Loaded API_URL:', API_URL);
console.log('Loaded DEALER_ID:', DEALER_ID);
console.log('Loaded DEALER_ID:', DEALER_UID);
export default { API_URL, DEALER_ID, DEALER_UID };